const { Client, MeesageEmbed, MessageAttachment } = require("discord.js")

module.exports = {
	name: "setup", 
	description: "",
	useage: "",
	run: (client, message, args) => {
		message.reply("Soon")
	}
}